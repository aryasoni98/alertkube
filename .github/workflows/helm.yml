name: Helm

on:
  push:
    branches: [master, main]
    paths:
      - "helm/**"
      - ".github/workflows/helm.yml"
  pull_request:
    branches: [master, main]
    paths:
      - "helm/**"
      - ".github/workflows/helm.yml"

permissions:
  contents: read

jobs:
  lint:
    name: Lint & Template
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Set up Helm
        uses: azure/setup-helm@v5
        with:
          version: v3.14.0

      - name: Helm lint
        run: helm lint helm

      - name: Helm template (default values)
        run: |
          helm template alertkube helm \
            --set cluster=ci-test \
            --set slack.webhookUrl=https://hooks.slack.com/services/T/B/X \
            > /tmp/render.yaml
          test -s /tmp/render.yaml

      - name: Helm template (servicemonitor on)
        run: |
          helm template alertkube helm \
            --set cluster=ci-test \
            --set slack.webhookUrl=https://hooks.slack.com/services/T/B/X \
            --set metrics.serviceMonitor.enabled=true \
            > /tmp/render-sm.yaml
          test -s /tmp/render-sm.yaml

      - name: Helm template (namespace scope + leader election)
        run: |
          helm template alertkube helm \
            --set cluster=ci-test \
            --set rbac.scope=namespace \
            --set replicaCount=2 \
            --set leaderElection.enabled=true \
            > /tmp/render-ns.yaml
          test -s /tmp/render-ns.yaml

      - name: kubeconform validation
        run: |
          curl -sSL https://github.com/yannh/kubeconform/releases/download/v0.6.7/kubeconform-linux-amd64.tar.gz | tar xz
          ./kubeconform -strict -ignore-missing-schemas -summary \
            /tmp/render.yaml /tmp/render-sm.yaml /tmp/render-ns.yaml
